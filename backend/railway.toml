[build]
builder = "nixpacks"
buildCommand = "pip install fastapi uvicorn pydantic python-dotenv torch transformers groq neo4j"

[deploy]
startCommand = "sh -c 'uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}'"