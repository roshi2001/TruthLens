[build]
builder = "nixpacks"
buildCommand = "pip install torch transformers fastapi uvicorn pydantic python-dotenv neo4j groq"

[deploy]
startCommand = "sh -c 'uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}'"